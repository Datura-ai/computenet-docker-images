[global]
# Ubuntu 24.04 marks the system interpreter "externally managed" (PEP 668); the
# image's python is the pod's, so installing into it is the intended use.
break-system-packages = true
# Keep torch at the build shipped in the image; see /etc/pip/constraints.txt.
constraint = /etc/pip/constraints.txt
